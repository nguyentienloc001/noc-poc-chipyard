# Secrets — TUYỆT ĐỐI không commit (xem src/docker/.env.example)
.env
*.env
!.env.example
!*/.env.example
!**/.env.example

# OS / editor noise
.DS_Store
__pycache__/
*.pyc

# Build artifacts (binary lớn — chỉ giữ source + log)
*.riscv
*.o
